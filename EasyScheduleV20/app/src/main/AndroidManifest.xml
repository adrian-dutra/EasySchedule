<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EasyScheduleV20"
        tools:targetApi="31">
        <activity
            android:name=".view.LocacaoQuadra.LocacaoQuadraActivity"
            android:exported="false" />

        <meta-data
            android:name="com.google.android.gms.ads.AD_MANAGER_APP"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".view.locatario.LocatarioActivity"
            android:exported="true"
            android:label="Locatário">

            <!-- Removido o intent-filter desnecessário -->
        </activity>
        <activity
            android:name=".view.locador.LocadorActivity"
            android:exported="true"
            android:label="Locador">

            <!-- Adicionado LocadorActivity -->
        </activity>
        <activity android:name=".view.quadras.QuadrasActivity" />
    </application>

</manifest>